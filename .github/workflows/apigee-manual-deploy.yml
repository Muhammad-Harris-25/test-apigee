name: Apigee - Manual Deploy

on:
  workflow_dispatch:
    inputs:
      proxies:
        description: "Type proxy names (comma-separated) OR ALL (see detected list in logs)"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ==========================================================
      # 1️⃣ CHECKOUT REPOSITORY
      # ==========================================================
      - name: Checkout repository
        uses: actions/checkout@v4

      # ==========================================================
      # 2️⃣ VERIFY & SHOW DETECTED PROXIES FILE
      # ==========================================================
      - name: Show detected proxies
        shell: bash
        run: |
          FILE=".github/detected_proxies.txt"

          if [ ! -f "$FILE" ]; then
            echo "❌ detected_proxies.txt not found in repo"
            echo "➡️ Run 'Apigee - Detect Changed Proxies' workflow first"
            exit 1
          fi

          if [ ! -s "$FILE" ]; then
            echo "❌ detected_proxies.txt is empty"
            exit 1
          fi

          echo ""
          echo "======================================"
          echo "🔍 DETECTED APIGEE PROXIES"
          echo "======================================"
          cat "$FILE"
          echo ""
          echo "➡️ Copy proxy names into workflow input"
          echo "➡️ Or type ALL"

      # ==========================================================
      # 3️⃣ RESOLVE USER SELECTION
      # ==========================================================
      - name: Resolve selected proxies
        shell: bash
        run: |
          DETECTED=$(cat .github/detected_proxies.txt)
          INPUT="${{ github.event.inputs.proxies }}"

          if [ -z "$INPUT" ]; then
            echo "❌ No proxies input provided"
            exit 1
          fi

          if [ "$INPUT" = "ALL" ]; then
            SELECTED="$DETECTED"
          else
            SELECTED=""
            IFS=',' read -r -a USER <<< "$INPUT"

            for p in "${USER[@]}"; do
              p=$(echo "$p" | xargs)
              if ! echo "$DETECTED" | grep -qw "$p"; then
                echo "❌ Invalid proxy: $p"
                echo "➡️ Must be one of:"
                echo "$DETECTED"
                exit 1
              fi
              SELECTED="$SELECTED $p"
            done
          fi

          if [ -z "$SELECTED" ]; then
            echo "❌ No valid proxies selected"
            exit 1
          fi

          echo "SELECTED_PROXIES=$SELECTED" >> $GITHUB_ENV

          echo ""
          echo "✅ Selected proxies:"
          for p in $SELECTED; do
            echo "- $p"
          done

      # ==========================================================
      # 4️⃣ LINT + DEPLOY + INTEGRATION
      # ==========================================================
      - name: Lint, Deploy & Integration
        shell: bash
        run: |
          for proxy in $SELECTED_PROXIES; do
            echo ""
            echo "🚀 Processing proxy: $proxy"

            PROXY_DIR="api-proxies/$proxy/apiproxy"
            POM_FILE="api-proxies/$proxy/pom.xml"
            TEST_FILE="api-proxies/$proxy/tests/integration/PassCollection.json"

            # ---------- LINT ----------
            apigeelint -s "$PROXY_DIR" --profile apigeex

            # ---------- DEPLOY ----------
            mvn clean install \
              -f "$POM_FILE" \
              -P$APIGEE_ENVIRONMENT \
              -Dorg=$ORG \
              -Dbearer=$ACCESS_TOKEN

            # ---------- INTEGRATION ----------
            if [ -f "$TEST_FILE" ]; then
              newman run "$TEST_FILE"
            fi
          done

      # ==========================================================
      # 5️⃣ FINAL SUMMARY
      # ==========================================================
      - name: Final Summary
        if: always()
        shell: bash
        run: |
          echo ""
          echo "======================================"
          echo "🚀 MANUAL DEPLOY SUMMARY"
          echo "======================================"
          echo ""
          for p in $SELECTED_PROXIES; do
            echo "✅ $p"
          done
